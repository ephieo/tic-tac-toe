jobs:
  build:
    docker:
      - image: "cimg/base:stable"
    steps:
      - checkout
      - ruby/install:
          version: "2.7"
      - run: echo "Ruby 2.7 has been installed"
orbs:
  ruby: circleci/ruby@x.y
version: 2.1
